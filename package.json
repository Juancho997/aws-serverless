{
  "name": "aws-serverless",
  "version": "1.0.0",
  "description": "Serveless implementation using AWS Services such as IAM User, Lambda, ApiGateway and DynamoDB",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Juan Bonino",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.363.0",
    "@aws-sdk/util-dynamodb": "^3.365.0",
    "serverless-iam-roles-per-function": "^3.2.0"
  }
}
